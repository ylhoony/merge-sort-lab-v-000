function findMinAndRemoveSorted (arr) {
  let min;
  
  arr.forEach((num) => {
min    if (min === undefined || num <= min) {
      min = num
    })
  }
  return 
}

function merge(firstArr, secondArr) {
  
}

function mergeSort() {
  
}
